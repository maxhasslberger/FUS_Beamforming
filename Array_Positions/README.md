Only applies to 3D simulations. 

Folder to store phased array positions of a given transducer as a mat file. For N array elements, the format has to be N x 3.

An array that defines the sparsity (indices) of the transducer may also be added (another mat file).
